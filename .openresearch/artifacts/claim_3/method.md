# Claim 3 method

This node first checks whether all exact task inputs needed for a matched
performance matrix are runnable. It does not run a smaller set and extrapolate.
Because six tasks are unavailable under the frozen environment, the exact
15-task comparison remains `BLOCKED`.

The existing one-task result reruns in the cumulative suite, including paired
bootstrap intervals and Wilcoxon tests, but is explicitly retained as partial
evidence only.

# Claim 2 method

The verifier enumerates all Table 1 rows. It reads the immutable GitHub tree
for the historical shapiq data commit with an explicit User-Agent, counts the
30 files for each precomputed paper task, and executes seed-1 constructors for
the three large TreeSHAP tasks using the author's random-forest and depth
fallback settings.

Tasks are counted runnable only when their exact input route is available.
Deleting one pinned manifest entry is the negative control and must make the
scope verifier fail.
